{
  "name": "qrlink",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "mocha --config test/.mocharc.cjs",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "license": "MIT",
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.0.6",
    "@types/qrcode": "^1.4.1",
    "gh-pages": "^3.2.3",
    "typescript": "^4.5.2",
    "vite": "^2.6.14"
  },
  "dependencies": {
    "qrcode": "^1.5.0"
  }
}
